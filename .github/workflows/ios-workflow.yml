name: iOS Native CI

on:
  workflow_dispatch:

jobs:
  ios-native:
    runs-on:
      - self-hosted
      - macOS
      - ARM64

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      - name: Start Appium
        run: |
          appium --allow-insecure chromedriver_autodownload > appium.log 2>&1 &

      - name: Wait for Appium
        run: |
          for i in {1..30}; do
            curl http://127.0.0.1:4723/status && exit 0
            sleep 2
          done
          exit 1

      - name: Verify iPhone
        run: xctrace list devices

      - name: Run Tests
        run: |
          mvn clean test \
            -DisRemote=false \
            -DtestSuite=src/main/resources/IOS.xml \
            -Dudid=${{ vars.IOS_UDID }}

      - name: Upload allure results
        uses: actions/upload-artifact@v4
        with:
          name: appium-allure-results
          path: target/allure-results